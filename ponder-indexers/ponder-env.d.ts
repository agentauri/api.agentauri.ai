// This file enables type safety for the Ponder config and schema.

import "@ponder/core";

declare module "@ponder/core" {
  export interface Register {
    // Define custom config types here if needed
  }
}
