# =============================================================================
# AgentAuri Backend - Test Environment Configuration
# =============================================================================
# Copy this file to .env.test and fill in your values:
#   cp .env.test.example .env.test
#
# Then source it before running integration tests:
#   source .env.test
#   cargo test
# =============================================================================

# =============================================================================
# DATABASE (PostgreSQL + TimescaleDB) - TEST ENVIRONMENT
# =============================================================================
# IMPORTANT: Use a separate test database, NOT your production database!
# Integration tests will create and delete test data.
#
# Recommended setup:
#   createdb agentauri_backend_test
#   sqlx migrate run --database-url postgresql://postgres:YOUR_PASSWORD@localhost:5432/agentauri_backend_test
#
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/agentauri_backend_test

# Alternative: Set PGPASSWORD for psql-based test scripts
PGPASSWORD=YOUR_PASSWORD

# =============================================================================
# REDIS - TEST ENVIRONMENT
# =============================================================================
# Use a different Redis database number for tests (default is 0)
REDIS_URL=redis://localhost:6379/1

# =============================================================================
# JWT AUTHENTICATION - TEST ENVIRONMENT
# =============================================================================
# Use a different secret for tests
JWT_SECRET=test_jwt_secret_not_for_production

# =============================================================================
# SECURITY NOTICE
# =============================================================================
# NEVER commit .env.test with real credentials to version control!
# The .env.test file is in .gitignore for your protection.
#
# For CI/CD:
# - Use GitHub Secrets or your CI provider's secret management
# - Never hardcode credentials in test files or scripts
# =============================================================================
