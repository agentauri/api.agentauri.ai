# Rust
target/
# Note: Cargo.lock is committed for reproducible builds (required for applications)
# Cargo.lock
**/*.rs.bk
*.pdb

# Node.js / TypeScript
node_modules/
dist/
.ponder/

# Environment variables
.env
.env.local
.env.*.local
.env.test  # Test environment credentials (NEVER commit!)
.env.backup*  # Environment backup files

# Database
*.db
*.sqlite
*.dump

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Docker
docker-compose.override.yml

# Build artifacts
*.wasm
*.bin

# Test coverage
coverage/
*.profdata
*.profraw

# Secrets
*.pem
*.key
/secrets/
.secret

# PostgreSQL TLS Certificates (development only)
docker/postgres/certs/*.crt
docker/postgres/certs/*.key
docker/postgres/certs/*.srl
docker/postgres/certs/*.pem

# Temporary files
tmp/
temp/
*.tmp

# Presentation files (not for commit)
ELEVATOR_PITCH.md

# Terraform
*.tfplan
tfplan
.terraform/
*.tfstate
*.tfstate.backup
# Environment-specific tfvars may contain secrets - use AWS Secrets Manager instead
terraform/environments/production.tfvars
terraform/environments/staging.tfvars
# Keep example files for reference
!terraform/environments/*.example.tfvars
